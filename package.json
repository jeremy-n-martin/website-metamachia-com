{
  "name": "metamachia-mns",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "compile": "npm run build && node dist/src/cli.js compile"
  },
  "devDependencies": { "typescript": "^5.7.3", "@types/node": "^22.10.2" }
}
